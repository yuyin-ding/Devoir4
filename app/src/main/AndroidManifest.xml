<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devoir4_2905">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BooksForU"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Devoir4_2905">
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".MoodActivity" />
        <activity android:name=".login.Create_AccountActivity"/>
        <activity android:name=".login.Reading_preferencesActivity"/>
        <activity android:name=".login.Reading_TimeActivity"/>
        <activity android:name=".login.MainActivity"
            android:label="BooksForU">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>